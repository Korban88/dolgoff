services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dolgoff
      POSTGRES_PASSWORD: dolgoff_dev
      POSTGRES_DB: dolgoff
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
